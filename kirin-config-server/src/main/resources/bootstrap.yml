spring:
  application:
    name: kirin-config-server
#  切换本地配置
#  profiles:
#    active: native

  cloud:
    config:
      server:
#       切换本地配置
#        native:
#          search-locations: C:/Users/ziv/Desktop/kirin-config
        git:
          uri: https://github.com/zivbob/kirin-config
          username: zivbob
          password: songhui1127